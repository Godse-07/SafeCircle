import 'package:flutter/material.dart';

class ParentHomescreen extends StatelessWidget {
  const ParentHomescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Parent Homescreen'),
      ),
    );
  }
}
